public class BinaryTree
{
    // variables
    private Node root;
    // root node
    // current node

    // constructor
    public BinaryTree()
    {
        root = null;
    }
    public void addDataRoot(int data)
    {
        if(root == null) {
            root = new Node(data);
        }
        else {
            root.setData(data);
        }
    }
    public void addData(int data, boolean left)
    {

    }



    // population methods
    // insert element
    // remove element

    // brain methods
    // transverse the tree
    //
}
